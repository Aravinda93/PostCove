# Nuxt + pnpm: hoist dependencies so Nuxt modules and Vite plugins resolve
# correctly under pnpm's strict, symlinked node_modules. Recommended by Nuxt.
shamefully-hoist=true
# Keep the project on a consistent toolchain.
engine-strict=false
